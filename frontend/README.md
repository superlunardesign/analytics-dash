# Analytics Dashboard -- Frontend

React + Vite + TypeScript dashboard UI. See the root `README.md` for full
setup instructions (Meta app setup, Render deploy, local dev).

Quick start:

```bash
npm install
cp .env.example .env.local   # VITE_API_BASE_URL=http://localhost:8000
npm run dev
```
